<section>
    <slot />
</section>

<style>
    section {
      grid-column: 1 / 13;
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--spacing-l);
    }

    @media (max-width: 899px) {
      section {
      grid-template-columns: 1fr;

    }
    }
</style>